(() => {
  // Background theme manager DISABLED to prevent conflicts with loading animation
  // This script has been disabled to allow the main loading system to work properly

  console.log(
    "Background theme manager disabled - using simple-bg-animation.js instead"
  );

  // All functionality moved to simple-bg-animation.js which respects loading animation timing
})();
