/**
 * Theme Animation Manager - DISABLED
 * This complex theme animation system has been disabled to prevent conflicts
 * with the main loading animation. All functionality moved to simple-bg-animation.js
 */
(() => {
  console.log(
    "Theme animation manager disabled - using simple-bg-animation.js instead"
  );

  // All functionality moved to simple-bg-animation.js which properly respects loading animation timing
})();
